# DCA reproduction: minimal deps for core + scripts
numpy>=1.17.0
pyyaml>=5.0
# Data prep (parquet output for VERL / one-click pipeline)
pandas>=1.1.0
pyarrow>=1.0.0
# Optional for full training pipeline (uncomment if using)
# torch>=1.12.0
# transformers>=4.30.0
# datasets

# Testing
pytest>=6.0
